flask==2.0.1
requests==2.25.1

app = Flask(__name__)

@app.route('/')
def index():
    return 'I am now a DevOps Engineer and ready to get into the job market'

if __name__ == '__main__':
    app.run(debug=True)
