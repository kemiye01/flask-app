pipeline {
    agent any

    stages {
        stage('Deploy Docker image with Ansible') {
            steps {
                script {
                   ansiblePlaybook credentialsId: 'ssh', disableHostKeyChecking: true, installation: 'ansible', inventory: 'host.ini', playbook: 'install.yml'
        }   }     }
    }
}