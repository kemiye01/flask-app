---
- name: Install pip on Ubuntu servers
  hosts: all
  become: true
  tasks:
    - name: Update package cache
      apt:
        update_cache: yes

    - name: Install pip
      apt:
        name: python3-pip
        state: present